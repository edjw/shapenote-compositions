# PDFs listed here are NOT copied to the blog repository by
# .github/workflows/pdf-webhook.yml.
#
# One path per line, exactly as it appears in git (relative to the
# repo root). Blank lines and lines starting with # are ignored.
#
# Example:
# compositions/finished/foo/foo.pdf
# compositions/finished/bar/bar.pdf

# No matching .ly file; would collide with the main Abide With Me PDF name.
compositions/finished/abide-with-me-16-5-26/original/abide-with-me.pdf

